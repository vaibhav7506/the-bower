# Self-hosted typefaces

The Latin variable WOFF2 files are checked in from the open-source Fontsource packages:

- `fraunces-latin.woff2`
- `fraunces-latin-italic.woff2`
- `inter-latin.woff2`

The CSS references only these local files and declares `font-display: swap`; no external font CDN is used.
